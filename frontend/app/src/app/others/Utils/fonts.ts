import { Lexend } from "next/font/google";

export const lexendBold = Lexend({
  weight: "700",
  subsets: ["latin"],
});

export const lexendMedium = Lexend({
  weight: "500",
  subsets: ["latin"],
});

export const lexendSmall = Lexend({
  weight: "300",
  subsets: ["latin"],
});
